# Gemini Instructions for contrarun

This file provides instructions for the Gemini AI to interact with the contrarun mini-app.

## Project Overview

This project is a Spixi mini-app with the ID `com.baracuda.spixi.contrarun`.

## General Instructions

-   The app's main logic is in `app/js/`.
-   The main HTML file is `app/index.html`.
-   The app's metadata is in `appinfo.spixi`.
-   When modifying this app, please follow the existing coding style and conventions.
-   Use the `SpixiAppSdk` and `SpixiTools` for all platform interactions.
